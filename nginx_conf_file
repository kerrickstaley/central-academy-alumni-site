server {
  listen 80;
  server_name caalum.kerrickstaley.com;
  root /home/nginx/caalum/static;

  location / {
    try_files $uri $uri.html $uri/index.html =404;
  }

  location ~ /api/.* {
    include fastcgi.conf;
    fastcgi_pass 127.0.0.1:9000;
  }
}
